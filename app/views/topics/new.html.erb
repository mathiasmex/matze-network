<h2><%=t ('topic.new_topic') %></h2>

<%= error_messages_for :topic %>

<% form_for :topic, :url => forum_topics_path(@forum)  do |f| %>
  <div class="form_row">
    <%= f.text_field :name %>
    <%= f.submit t('global.submit'), :class => "button" %>
  </div>
<% end %>
